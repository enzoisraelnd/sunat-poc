{
  "name": "sunat-factoring-service",
  "version": "1.0.0",
  "description": "Factoring service - SUNAT SIRE invoice consultation",
  "scripts": {
    "build": "sam build --config-env dev",
    "deploy": "sam deploy --config-env dev --profile toqueadev --region us-east-1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^20.17.0",
    "@types/uuid": "^10.0.0",
    "esbuild": "^0.27.3",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/client-secrets-manager": "^3.700.0",
    "@aws-sdk/client-ses": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "uuid": "^10.0.0"
  }
}